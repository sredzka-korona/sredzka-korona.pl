{
  "name": "sredzka-korona",
  "private": true,
  "type": "module",
  "scripts": {
    "preview": "node scripts/preview.mjs",
    "seo:generate": "node scripts/generate-seo.mjs",
    "d1:clear-reservations": "cd worker && npx --yes wrangler@4 d1 execute sredzka-korona --remote --file ../scripts/d1/clear-reservations.sql",
    "worker:dev": "cd worker && npm run dev",
    "worker:deploy": "cd worker && npm run deploy",
    "worker:secret-turnstile": "cd worker && npm run secret:turnstile"
  }
}
